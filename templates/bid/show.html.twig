
{% extends 'base.html.twig' %}
{% block title %}Bid{% endblock %}

{% block body %}
    <h1>Bid</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idbid</th>
                <td>{{ bid.idbid }}</td>
            </tr>
            <tr>
                <th>Bidamount</th>
                <td>{{ bid.bidamount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bid_index') }}">back to list</a>

    <a href="{{ path('app_bid_edit', {'idbid': bid.idbid}) }}">edit</a>

    {{ include('bid/_delete_form.html.twig') }}
{% endblock %}
